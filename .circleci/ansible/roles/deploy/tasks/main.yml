 - name: "Unarchive backend files"
   become: true
   unarchive:
       src: artifact.tar.gz
       dest: .circleci

 - name: "Executing Node app with PM2"
   shell: |
     npm install
     pm2 stop default
     pm2 start npm -- start 
       