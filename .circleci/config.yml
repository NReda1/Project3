version: 2.1

jobs:
  build-frontend:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:
          command: |
             cd /c/nano\ degree/CICD/Project3-1/frontend/
             npm install
             npm run build

  build-backend:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:
          command: |
             cd /c/nano\ degree/CICD/Project3-1/backend/
             npm install
             npm run build
    
workflows:
  default:
    jobs:
      - build-frontend
      - build-backend
      